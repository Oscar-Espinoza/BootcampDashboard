import styled from "styled-components";

export const StyledInput = styled.input`
    border: none;
    font-size: 15px;
    width: 120px;
    &:focus {
      outline: none;
      border: none;
    }
`;